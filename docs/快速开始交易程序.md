# 🚀 快速开始交易程序

## 前置要求

**必须安装tmux**（终端复用器，用于后台运行程序）：
```bash
# 检查是否已安装
tmux -V

# 如果未安装，执行安装
apt update && apt install -y tmux
```

## 启动交易程序

```bash
cd /root/duobizhong
./scripts/start_portfolio.sh
```

## 停止交易程序

### 方法1：直接杀死进程（推荐）

```bash
# 停止交易程序
pkill -f portfolio_manager.py

# 停止看板程序
pkill -f web_app.py
```

### 方法2：在tmux中停止

```bash
# 1. 进入tmux会话
tmux attach -t portfolio

# 2. 停止程序
Ctrl+C

# 3. 退出会话
exit
```

## 查看交易程序

```bash
# 连接到tmux会话查看实时日志
tmux attach -t portfolio

# 断开会话（程序继续运行）
Ctrl+B 然后按 D
```

## 查看日志文件

```bash
# 实时查看
tail -f /root/duobizhong/portfolio_manager.log

# 查看最近100行
tail -n 100 /root/duobizhong/portfolio_manager.log
```

## 测试模式 vs 实盘模式

编辑 `portfolio_manager.py` 第156行：

```python
PORTFOLIO_CONFIG = {
    'leverage': 3,
    'check_interval_minutes': 5,  # 5分钟调用一次AI（⚠️ 不建议修改，匹配15分钟K线）
    'test_mode': False  # True=测试模式（不真实下单）, False=实盘模式
}
```

**⚠️ 重要说明：**
- `check_interval_minutes`: 默认5分钟，**不建议修改**
- 程序使用 **15分钟+1小时+4小时** 多时段分析
- 5分钟间隔可以完美捕获15分钟K线的变化
- 修改此值会导致K线数据不匹配，影响AI判断准确性

## 快速命令对照表

| 操作 | 命令 |
|------|------|
| 启动 | `cd /root/duobizhong && ./scripts/start_portfolio.sh` |
| 停止 | `pkill -f portfolio_manager.py` |
| 查看 | `tmux attach -t portfolio` |
| 断开 | `Ctrl+B` + `D` |
| 重连 | `tmux attach -t portfolio` |
| 日志 | `tail -f /root/duobizhong/portfolio_manager.log` |

---

**注意**：首次运行建议先设置 `test_mode=True` 观察AI决策。
