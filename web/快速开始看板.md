# 🚀 快速开始看板程序

## 启动看板程序

```bash
cd /root/ziyong/duobizhong/web
./start_web.sh
```

## 停止看板程序

```bash
pkill -f web_app.py
```

## 查看看板日志

```bash
# 实时查看
tail -f /root/ziyong/duobizhong/web/web_app.log

# 查看最近100行
tail -n 100 /root/ziyong/duobizhong/web/web_app.log
```

## 访问看板（SSH隧道）

### 在本地电脑执行（保持连接）

```bash
ssh -L 5000:localhost:5000 root@your-server-ip
```

### 浏览器访问

```
http://localhost:5000
```

## 快速命令对照表

| 操作 | 命令 |
|------|------|
| 启动 | `cd /root/ziyong/duobizhong/web && ./start_web.sh` |
| 停止 | `pkill -f web_app.py` |
| 日志 | `tail -f /root/ziyong/duobizhong/web/web_app.log` |
| SSH隧道 | `ssh -L 5000:localhost:5000 root@your-server-ip` |

---

**注意**：
- 看板只用于监控，不执行交易
- 需要先启动交易程序，看板才有数据
- SSH隧道需要保持连接
